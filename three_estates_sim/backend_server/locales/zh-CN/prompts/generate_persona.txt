你正在为一款回合制社交推理智能体模拟器生成人物资料。模拟器会根据人物性格生成对白，并让人物循环作出行动决策。

完整人物名单已经在独立步骤中确定：
{full_roster}

本次只为以下固定名单成员生成资料：
{fixed_name}

人物及整个角色组的初始背景：
{character_group_context}

名单选择步骤已经把所有简称补全为规范完整显示名。因此 `name` 字段必须严格等于 `{fixed_name}`；人物资料生成阶段不得再改名、替换、翻译、调整顺序、缩短、扩写或加入别名。若背景涉及已有作品、系列、世界观、跨作品组合或明确角色名单，应优先准确采用原作人物设定。

只填写以下 JSON 字段：
- `name`：严格逐字使用已经规范化的固定名单姓名。
- `gender`：协议值只能是全小写英文 `male` 或 `female`；确实无法判断时可用 `unknown`。
- `age`：整数。通常填写合理的人类年龄；若背景明确是幻想、长生、远古、人造、宇宙或其他非人角色，可以使用异常年龄。
- `innate`：一至两段第三人称资料。当前语言的长度限制为：{innate_profile_limit}。内容严格分为两类持久信息：（1）人物自身的个人经历与当前个人状态，必要时包括已经确立的人际／恋爱关系；（2）仅描述其稳定性格在社交推理游戏中的表现，例如说话方式、推理习惯、脾气、诈唬方式、社交主动性与猜疑程度。输入的角色组背景只可用于确认人物身份及提取持久的原作／既定事实，不得把临时场景包装写进人物资料：不要写其参加了本局游戏、正身处这家店或本局地点、初次见到或已经熟悉本局这批参与者、面对这些特定参与者感到拘谨、当前坐在哪桌或持有什么身份，也不要写任何仅由本次输入游戏背景创造的临时处境。应描述跨局稳定的倾向，而非某一局的情境。不要写外貌或服装。

必须采用以下 JSON 结构：
{{
  "name": "字符串",
  "gender": "female",
  "age": 18,
  "innate": "字符串"
}}

只返回有效 JSON 对象。必须使用双引号包裹 JSON 键和字符串；不要使用 Markdown 代码块、注释、尾随逗号或未转义的英文双引号。
