你正在为一款回合制社交推理智能体模拟器生成人物资料。模拟器会根据人物性格生成对白，并让人物循环作出行动决策。

完整人物名单已经在独立步骤中确定：
{full_roster}

本次只为以下固定名单成员生成资料：
{fixed_name}

人物及整个角色组的初始背景：
{character_group_context}

名单选择步骤已经把所有简称补全为规范完整显示名。因此 `name` 字段必须严格等于 `{fixed_name}`；人物资料生成阶段不得再改名、替换、翻译、调整顺序、缩短、扩写或加入别名。若背景涉及已有作品、系列、世界观、跨作品组合或明确角色名单，应优先准确采用原作人物设定。

只填写以下 JSON 字段：
- `name`：严格逐字使用已经规范化的固定名单姓名。
- `gender`：协议值只能是全小写英文 `male` 或 `female`；确实无法判断时可用 `unknown`。
- `age`：整数。通常填写合理的人类年龄；若背景明确是幻想、长生、远古、人造、宇宙或其他非人角色，可以使用异常年龄。
- `innate`：一至两段第三人称资料。当前语言的长度限制为：{innate_profile_limit}。说明其在当前大背景中的经历与核心性格，尤其关注说话方式、思考方式、气质、猜疑程度及社交倾向。不要写外貌或服装。

必须采用以下 JSON 结构：
{{
  "name": "字符串",
  "gender": "female",
  "age": 18,
  "innate": "字符串"
}}

只返回有效 JSON 对象。必须使用双引号包裹 JSON 键和字符串；不要使用 Markdown 代码块、注释、尾随逗号或未转义的英文双引号。
