You are generating the profile of a character for use in a turn-based agent simulation of a social deduction game. The simulation includes personality-based spoken lines and action decision loops.

The complete cast roster has already been selected in a separate pass:
{full_roster}

Generate the profile for this exact roster member:
{fixed_name}

Given that the assumed context for this character and the rest of the cast is:
{character_group_context}

The roster-selection pass has already resolved every shorthand into its canonical
full display name. The `name` field MUST therefore be exactly `{fixed_name}`. Do not
rename, replace, translate, reorder, shorten, expand, or add an alias to it during
profile generation. Prioritize established/canonical characterization if the
context refers to a known work, franchise, setting, crossover, or explicit list.

You must fill out the below dictionary, formatted exactly like below with no empty lines:

name:
gender: male or female, all lower case
age: integer; normally use a plausible human age, but if the context clearly calls for fantasy, immortal, ancient, artificial, cosmic, or otherwise nonhuman characters, unusual ages are allowed
innate: (one paragraph or two in third person. Length limit for the active language: {innate_profile_limit}. Explain their background in the broader context and core personality, especially related to dialogue, chain of thought, temperament, or degree of suspicion.)

Required JSON shape:
{{
  "name": "string",
  "gender": "female",
  "age": 18,
  "innate": "string"
}}

Return ONLY a valid JSON object using double quotes for JSON keys and string delimiters. Do not use markdown fences, comments, trailing commas, or unescaped double quotes inside string values.
